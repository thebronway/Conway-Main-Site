<nav>
  <div class="logo"><a href="/">brian.conway.im</a></div>
  <div class="links">
    <a href="/">Home</a>
    <a href="/projects/">Projects</a>
    <a href="/sites/">Sites</a>
  </div>
</nav>

<style>
  nav { 
    display: flex;
    justify-content: space-between; 
    align-items: center;
    padding: 1.5rem 2rem; 
    max-width: 1064px;        /* LOCK: Matches the exact width of your card grid */
    width: 100%;
    box-sizing: border-box;
    margin: 0 auto;           /* Centers the navbar contents inside the wider wrapper */
  }
  .logo { 
    font-weight: 800; 
    font-size: 1.2rem;
    letter-spacing: -0.05em; 
    white-space: nowrap;
    flex-shrink: 0;
  }
  .links {
    display: flex;
    gap: 1.5rem;
    align-items: center;
  }
  .links a { 
    color: var(--text-muted); 
    font-weight: 500; 
    transition: color 0.2s;
    white-space: nowrap;
  }
  .links a:hover { color: var(--text-main); }

  @media (max-width: 768px) {
    nav {
      flex-direction: column;
      gap: 1rem;
    }
    .links {
      flex-wrap: wrap;
      justify-content: center;
      gap: 1rem;
    }
  }
</style>