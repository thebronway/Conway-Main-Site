---
import StatusBadge from './StatusBadge.astro';
const { site } = Astro.props;
const { title, description, status, url, heroImage } = site.data;
const detailsUrl = `/sites/${site.slug}/`;
---

<div class="card">
  {heroImage && (
    <a href={detailsUrl} class="image-link">
      <img src={heroImage} alt={title} class="hero-image" />
    </a>
  )}
  <div class="content">
    <a href={detailsUrl} class="title-link">
      <h3 class="title">{title}</h3>
    </a>
    
    <div class="status-wrapper">
      <StatusBadge status={status} />
    </div>

    <p class="description">{description}</p>
    
    <div class="actions">
      <div class="primary-actions">
        {url && <a href={url} target="_blank" class="btn btn-white">Visit ↗</a>}
      </div>

      <a href={detailsUrl} class="btn btn-dark">Details →</a>
    </div>
  </div>
</div>

<style>
  .card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 12px;
    overflow: hidden;
    transition: transform 0.2s, border-color 0.2s;
    display: flex;
    flex-direction: column;
    height: 100%;
    min-height: 220px;
  }

  .card:hover {
    transform: translateY(-4px);
    border-color: var(--accent);
  }

  .image-link {
    display: block;
  }

  .hero-image {
    width: 100%;
    height: 160px;
    object-fit: cover;
    border-bottom: 1px solid var(--border);
  }

  .content {
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    flex-grow: 1;
  }

  .title-link {
    text-decoration: none;
  }

  .title {
    margin: 0;
    font-size: 1.5rem;
    color: #fff;
    line-height: 1.2;
    margin-bottom: 0.75rem;
    transition: color 0.2s;
  }

  .title-link:hover .title {
    color: var(--accent);
  }

  .status-wrapper { margin-bottom: 1rem; }

  .description {
    color: var(--text-muted);
    font-size: 0.95rem;
    line-height: 1.5;
    margin-bottom: 1.5rem;
    flex-grow: 1;
  }

  .actions {
    margin-top: auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    padding: 0.5rem 1rem;
    border-radius: 6px;
    font-size: 0.9rem;
    font-weight: 600;
    transition: all 0.2s;
    text-decoration: none;
    border: 1px solid transparent;
  }

  /* WHITE BUTTON (Visit) */
  .btn-white {
    background: #fff;
    color: #0f172a;
    border-color: #fff;
  }
  .btn-white:hover {
    background: #e2e8f0;
    border-color: #e2e8f0;
  }

  /* DARK BUTTON (Details) */
  .btn-dark {
    background: rgba(255,255,255,0.05);
    color: #94a3b8;
    border-color: var(--border);
  }
  .btn-dark:hover {
    color: #fff;
    border-color: var(--accent);
    background: rgba(255,255,255,0.1);
  }
</style>